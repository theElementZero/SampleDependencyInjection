<UserControl x:Class="WpfSampleDi.UserControlExample"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:WpfSampleDi.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:ViewModel1}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding UriString, Mode=TwoWay}" Grid.Column="0" />
            <Button Grid.Column="1" Content="Set Address" Command="{Binding SetUriCommand, Mode=OneTime}" />
        </Grid>
        <Frame Grid.Row="1" Source="{Binding ActualUri, Mode=OneWay}" />
    </Grid>
</UserControl>
